주요기능 - 서브메뉴, 전체메뉴, 슬라이드, 스크롤 애니메이션
고민했던 부분 - 각 섹션별 높이가 달라 어디에서 class를 추가할 것인지,
스크롤 시 class를 추가하는 코드를 간략하게 정리하고 싶었다
해결방법 - 기존에 index값을 각각 지정했던 코드를 섹션의 탑값과 섹션의 높이값을 가져와 공통으로 클래스를 추가하였고,
높이가 낮은 섹션만 따로 지정해줌
let start = secT - (secH * 0.7); 섹션의 시작위치 - (섹션 높이의 70%)
let end = secT + secH; 섹션의 시작위치 + 섹션의 높이 = 섹션 전체 높이
if(scrollY >= start && scrollY < end) 